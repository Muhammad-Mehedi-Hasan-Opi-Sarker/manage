import React from 'react';
import { Button } from 'react-bootstrap';
import Navbar from './Navbar';

const Home = () => {
    return (
        <div>
           <Navbar></Navbar>
        </div>
    );
};

export default Home;